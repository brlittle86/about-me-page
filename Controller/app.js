'use strict';

console.log('Successfully linked with index.html');

var dog = prompt('Tell me a thing');

if (dog.toLowerCase() === ('potato' || 'grass')) {
  console.log('They like the stuff');
} else {
  console.log('bad juju');
}